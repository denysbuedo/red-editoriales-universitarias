# Catalog domain layer

Contiene entidades, aggregates, Value Objects, eventos y errores del dominio editorial.

Reglas actuales:

- no depende de Next.js;
- no depende de base de datos;
- no realiza llamadas HTTP;
- valida invariantes en constructores o fabricas.
